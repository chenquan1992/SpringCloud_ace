package ${package}.mapper;

import java.util.List;
import java.util.Map;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Mapper extends MyMapper<${className}> {

    /**
    * 根据条件查询，主键倒序排序
    */
    List<${className}> selectByQuery(Map<String, Object> map);

}
